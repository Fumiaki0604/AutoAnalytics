以下の分析依頼に対して、検証可能な仮説を **3つ以上** 生成してください。

## 過去の分析コンテキスト

{past_context}

過去の発見は**参考情報**として扱い、今回のデータで改めて検証してください。過去の発見が今回のデータと一致しない場合は、今回のデータを優先してください。過去の発見を前提条件として固定しないこと。

## 過去のSQLエラー履歴

{corrections_context}

上記のエラーパターンと同じ SQL を生成してはならない。

---

## 分析依頼の概要
{summary}

## 元の依頼文（原文）
{raw_request}

## 対象 KPI
{kpi}

## 分析の切り口
{dimensions}

## 利用テーブル
{table}

## テーブル情報（スキーマ・サンプル・値域）
{data_context}

---

## 分析スコープの解釈ルール（必読）

**「把握済み・確認済み・既知」情報の扱い**

ユーザーが「○○と把握済みだが」「○○は確認済み」「○○が増加していることはわかっているが」等と前置きした情報は**背景コンテキスト**であり、仮説の WHERE 句で絞り込む対象ではない。

- ❌ 悪い例: 「ProductDetail.aspxが増加と把握済みだが、どのページが増えたか確認してほしい」
  → 全仮説を `WHERE landingPage LIKE '%ProductDetail.aspx%'` に絞り込む
- ✅ 良い例:
  → 仮説1: 絞り込みなしで全ランディングページのセッション増減を集計（全体俯瞰）
  → 仮説2以降: 把握済み情報（ProductDetail.aspx）の詳細検証

**「どの○○が」「全体の内訳」「全体を把握したい」等の依頼**

GROUP BY で全データを網羅する仮説を最初に生成すること。背景として言及された特定要素に全仮説を絞り込んではならない。

---

## SQL を書くうえでの厳守ルール

1. **日付・値は必ず上記「値域」に記載された実際の値を使うこと。** 推測・架空の値を WHERE 句に入れてはならない。
2. **日付フィルタは `BETWEEN` や `>=` / `<=` を使い、値域内の範囲を指定すること。** 分析依頼の冒頭に `[重要: 取得データは YYYY-MM-DD 〜 YYYY-MM-DD ...]` が明示されている場合は、その期間を厳守し、範囲外の日付（例: 前月、前四半期など）を含めてはならない。
3. **カラム名は「スキーマ」に記載されたものだけを使うこと。** 存在しないカラムを参照してはならない。
4. **DuckDB 構文を使うこと。** `STRFTIME` ではなく `STRPTIME` / `DATE_TRUNC` / `date_part` を使う。
5. SQL が長くなる場合は CTE（WITH 句）で読みやすく書くこと。
6. **CTE 名に DuckDB 予約語を使ってはならない。** 以下の名称は使用禁止。代わりに `_data` や `_result` を末尾に付けた名称を使うこと。
   - 禁止: `pivot`, `unpivot`, `rank`, `ranked`, `sample`, `filter`, `exclude`, `values`, `table`, `index`, `group`, `order`, `select`, `from`, `where`, `join`, `on`, `as`, `by`, `with`
   - 例: `pivot` → `channel_pivot_data`、`ranked` → `ranked_pages`、`monthly_device` → `device_monthly_data`

---

## 出力形式（この形式を厳守してください）

各仮説を以下の形式で番号付きリストとして記述してください。

1. [仮説タイトル（一文）]
   説明: 仮説の内容と、そう考える根拠
   ```sql
   -- DuckDB で実行可能な検証 SQL（実データの値域に基づいた WHERE 句を使う）
   SELECT
     ...
   FROM {table}
   ...
   LIMIT 20;
   ```

2. [仮説タイトル]
   ...

3. [仮説タイトル]
   ...
